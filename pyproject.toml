[project]
name = "cascade-configuration-service"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "streamlit",
    "pandas",
    "graphviz",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
